/**
 * Vue Router 路由配置
 * 
 * 路由结构：
 *   默认路径 (/) 为用户界面
 *   /admin 开头为管理员界面
 * 
 * 用户路由：
 *   /login           - 用户登录页
 *   /                - 用户仪表盘
 *   /configs         - 查看与下载配置
 *   /clients         - 客户端下载
 * 
 * 管理员路由：
 *   /admin/login     - 管理员登录页
 *   /admin           - 管理员仪表盘
 *   /admin/configs   - 配置管理（CRUD）
 *   /admin/configs/:id/edit - 在线编辑配置
 *   /admin/clients   - 客户端下载
 */
